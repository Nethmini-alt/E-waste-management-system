API_BASE_URL=http://localhost:5172
AGENT_API_KEY=
# This service's own key — see the team's multi-key plan, one per agent that
# actually calls an LLM, so a busy Analyzer can't starve Planner's quota.
GOOGLE_API_KEY=
CHAT_MODEL=gemini-1.5-flash
